[project]
name = "zyth-core"
version = "0.1.0"
description = "Zyth compiler core"
requires-python = ">=3.10"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
